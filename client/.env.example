# Frontend API URL (use NEXT_PUBLIC_ prefix for client-side exposure)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Push Notifications (Web Push) - Public key exposed to client
# Must match the VAPID_PUBLIC_KEY in server .env
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key_here
